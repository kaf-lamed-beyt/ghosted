export const {
  NEXT_PUBLIC_APP_URL,
  GITHUB_CLIENT_ID,
  GITHUB_CLIENT_SECRET,
  PGHOST,
  PGPORT,
  PGDATABASE,
  PGUSERNAME,
  PGPASSWORD,
  PGSSLMODE,
  CRY_KEY,
  NODE_ENV,
  RESEND_KEY,
  GITHUB_PAT,
  EMAIL_DOMAIN,
} = process.env;
